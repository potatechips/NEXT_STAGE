<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<%= render 'layouts/sidebar', goal: @goal_new, user: current_user %>
		</div>
	    <div class="col-xs-9">
	    	<%= render 'goals/goal_table', goals: @goals %>
	    	<%= paginate @goals %>
	    </div>
	</div>
</div>
