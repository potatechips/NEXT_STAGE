<div class="container">
	<div class="row">
	    <div class="col-xs-3">
			<%= render 'layouts/sidebar', goal: @goal_new, user: current_user %>
		</div>
		<div class="col-xs-9">
		 	<%= render 'users/user_table', users: @users %>
	    	<%= paginate @users %>
	    </div>
	</div>
</div>